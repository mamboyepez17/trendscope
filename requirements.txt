# Synced from pyproject.toml — single source of truth is pyproject.toml

# Scraping adaptativo
scrapling
requests>=2.31.0

# Reddit
praw>=7.8.1

# Google Trends fallback
pytrends>=4.9.2

# Sentimiento español latinoamericano + ingles (autodeteccion de idioma)
# Nota: pysentimiento requiere torch, que puede fallar en algunos entornos.
# Si pysentimiento no instala/carga, TrendScope usa fallback por keywords automaticamente.
pysentimiento>=0.7.3

# Claude API (sentimiento premium)
anthropic>=0.25.0

# API REST
fastapi>=0.111.0
uvicorn>=0.30.0

# MCP server
mcp[cli]>=1.0.0

# Utils
python-dotenv>=1.0.1
loguru>=0.7.2
rich>=13.7.1
pydantic-settings>=2.0.0
httpx>=0.27.0
tenacity>=8.3.0
apscheduler>=3.10.0
websockets>=12.0
ollama>=0.2.0
openpyxl>=3.1.0
